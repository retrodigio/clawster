# dependencies
node_modules

# output
dist
out
*.tgz

# env files (secrets)
.env
.env.*
!.env.example

# config (user-specific, lives in ~/.clawster/)
config/agents.json

# logs
logs
*.log

# scratch from the retired Playwright/CDP browser design (see CLAUDE.md).
# `*.log` already covered the console dumps; the page-*.yml accessibility
# snapshots were not, and some captured logged-in pages.
.playwright-mcp/

# caches
.eslintcache
.cache
*.tsbuildinfo

# OS
.DS_Store

# IDE
.idea
.vscode
